_format_version: "3.0"

plugins:
  - name: prometheus
    enabled: true
    config:
      per_consumer: true
      status_code_metrics: true
      latency_metrics: true
      bandwidth_metrics: true
      upstream_health_metrics: true

services:

  #backend service routes
  - name: peekup_pay_backend
    url: http://peekup_pay_backend:8000
    routes: