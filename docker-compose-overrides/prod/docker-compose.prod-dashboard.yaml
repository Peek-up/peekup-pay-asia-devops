include:
 - ./common/monitoring/prod/docker-compose.prod.yaml
 - ./peekup-pay/dashboard/docker-compose.yaml
services:
  caddy:
    extends:
      file: common/caddy/docker-compose.yaml
      service: caddy
    volumes:
      - ./caddyfile-overrides/prod/Caddyfile.prod-dashboard:/etc/caddy/Caddyfile

volumes:
  caddy_data:
  caddy_config:

