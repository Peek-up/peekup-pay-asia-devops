include:
 - ./common/monitoring/uat/docker-compose.uat.yaml
 - ./peekup-pay/dashboard/docker-compose.yaml
services:
  caddy:
    extends:
      file: common/caddy/docker-compose.yaml
      service: caddy
    volumes:
      - ./caddyfile-overrides/uat/Caddyfile.uat-dashboard:/etc/caddy/Caddyfile

volumes:
  caddy_data:
  caddy_config:
